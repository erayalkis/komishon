// Returns the last element of an array
// TODO: Make all lines using this function switch to the `at` function.
export function lastEleOf(arr) {
  const lastEleIdx = arr.length - 1;
  return arr[lastEleIdx];
}
